# AI Agent Instructions

Read `project.yml` before changing this project. Inspect relevant code, tests, and documentation before edits; ask about material ambiguity rather than inventing requirements or APIs.

Preserve the existing architecture and its `api`, `application`, `domain`, `infrastructure`, and `config` boundaries. Follow TDD when practical, add no unnecessary dependencies, update Markdown and Mermaid documentation when affected, and report only validations actually executed.